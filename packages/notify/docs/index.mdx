---
title: notify
order: 1
---

import { Table } from 'antd';
import CodeDemo from '@theme/CodeDemo';
import Demo from './Demo.tsx';
import ThemeNotifyDemo from './ThemeNotifyDemo.tsx';

通知器, 解决跨模块依赖问题

import ImportTabs from '@theme/ImportTabs';

### 安装
<ImportTabs name="@jelper/notify" />

### 样例

#### notify 使用
<CodeDemo title="通用notify" desc="" text={$include('./Demo.tsx')}><Demo /></CodeDemo>

#### themeNotify 使用
<CodeDemo title="系统主题notify" desc="" text={$include('./ThemeNotifyDemo.tsx')}><ThemeNotifyDemo /></CodeDemo>

### type

``` typescript
type MoudleType = <T = void>() => {
    notify: (data: T) => void;
    on: (callback: (data: T) => void) => Unsubscribe;
    once: (callback: (data: T) => void) => Unsubscribe;
    destroy: () => void;
};
```
