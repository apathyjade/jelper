---
title: useInterval
sort: 1
---

import CodeDemo from '@theme/CodeDemo';
import ImportTabs from '@theme/ImportTabs';
import ApiTable from '@theme/ApiTable';
import Demo from './Demo.tsx';

### 安装

<ImportTabs name="@jelper/hooks" />

### 引入

``` tsx
import { useInterval } from '@jelper/hooks';
```

### 样例

#### 基础用法
<CodeDemo title="基础用法" desc="" text={$include('./Demo.tsx')}><Demo /></CodeDemo>

### API

<ApiTable
  dataSource={[
    {
      param: 'useInterval',
      desc: '使用 useEffect 封装 interval',
      type: 'UseInterval',
      defValue: '--',
      version: '',
    },
    {
      param: 'useIntervalHandler',
      desc: '使用 interval 句柄',
      type: 'UseIntervalHandler',
      defValue: '--',
      version: '',
    },
  ]}
/>

#### Interface

``` tsx
type UseInterval = (callback: Function, timeout?: number, ...arg: any[]) => (() => void);
type UseIntervalHandler = () => [
  (callback: Function, timeout?: number, ...arg: any[]) => void, // 绑定事件
  () => void, // 清除事件
];
```

