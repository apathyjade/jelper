---
name: Hello world
---

*a*

import gc from '@jelper/common-css';

<div className={`${gc['text-xs']} ${gc['bg-test']} ${gc['test']}`}>测试下</div>
<div className={`${gc['text-s']}  ${gc['bg-blue']} ${gc['blue']}`}>测试下</div>
<div className={`${gc['text-m']}  ${gc['bg-green']} ${gc['green']}`}>测试下</div>
<div className={`${gc['text-l']}  ${gc['bg-orange']} ${gc['orange']}`}>测试下</div>
<div className={`${gc['text-xl']}  ${gc['bg-yellow']} ${gc['yellow']}`}>测试下</div>

## 样例
```jsx title="/src/components/HelloCodeTitle.js"

<>
  
</>
```

